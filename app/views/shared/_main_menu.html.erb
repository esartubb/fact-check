<ul>
	<%= content_tag :li do %>
		<%= link_to(t('users_index_title'), users_path) %>
	<% end if current_user_has_role? :admin %>
	<%= content_tag :li do %>
		<%= link_to(t('log_in'), new_user_session_path) unless user_signed_in? %>
		<%= link_to(t('log_out'), destroy_user_session_path, method: :delete) if user_signed_in? %>
	<% end %>
</ul>